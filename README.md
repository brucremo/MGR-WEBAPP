# MGR Web App

- Repo synced with Heroku, each new commit will automatically deploy the changes to the app
- Run npm install and node server.js for local testing, then browse to localhost:8080
- ./node_modules | ./dist on .gitignore 
